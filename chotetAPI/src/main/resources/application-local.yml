spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/chotet
    username: chotet
    password: chotet
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false

server:
  port: 8080
